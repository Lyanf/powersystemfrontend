import { construct, destruct } from '@aximario/json-tree';
function oriDataToMetaDataTree(oriDataList) {
  var useJsonList = [];
  for(let everyData of oriDataList){
    let factory = everyData.factory;
    let line = everyData.line;
    let device = everyData.device

  }
}
